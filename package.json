{
  "name": "final-project-e-shop",
  "version": "1.0.0",
  "description": "e-shop using node react mongoDB",
  "engines": {
    "npm": "6.4.1",
    "node": "10.15.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && npm install --no-optional --prefix server && npm start --prefix server",
    "build": "npm install --no-optional --prefix client && npm run build --prefix client"
    },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ekrechetov/final-project-e-shop.git"
  },
  "dependencies": {}
}
