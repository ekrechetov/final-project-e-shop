{
  "name": "final-project-e-shop",
  "version": "1.0.0",
  "description": "e-shop using node react mongoDB",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install --no-optional --prefix server && npm start --prefix server",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client  && rm -rf /server/build/* && cp -a /client/build/. /server/build/"
    },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ekrechetov/final-project-e-shop.git"
  },
  "dependencies": {}
}
